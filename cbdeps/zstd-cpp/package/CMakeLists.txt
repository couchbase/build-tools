IF (WIN32)
    FILE (MAKE_DIRECTORY "${CMAKE_INSTALL_PREFIX}/bin")
    FILE (COPY bin/zstd.dll DESTINATION "${CMAKE_INSTALL_PREFIX}/bin")
ENDIF ()

FILE (COPY lib DESTINATION "${CMAKE_INSTALL_PREFIX}")
